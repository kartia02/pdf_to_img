# SinglePg — 이력서용 AI 에이전트 프로젝트 기획 노트

> **문서 사용법**
> 이 문서는 두 층으로 나뉩니다.
> - **결정 층** (아래 "현재 상태" ~ "설계 원칙"): 지금 사실인 것만 적습니다. 바뀌면 **덮어씁니다.**
> - **경위 층** (맨 아래 "진행 로그"): 어떻게 여기까지 왔는지의 기록. **절대 수정하지 않고 계속 덧붙입니다.**
>
> 새로 작업할 때는 "현재 상태"만 읽으면 되고, "왜 이렇게 됐지?"가 궁금할 때만 진행 로그를 봅니다.
> (2026-08-12 시작)

---

## 현재 상태 (최종 수정: 2026-08-18)

**한 줄 요약**: PDF 업로드 → 카드뉴스 슬라이드 자동 생성까지 전 구간이 동작하는 상태. 웹 UI 포함.

### 동작하는 것
- PDF 업로드 → 파싱 → 기획 → 이미지 생성 → QA 검수 → (탈락 시) 재생성 → 웹 갤러리 표시
- QA 자체검토 루프: 슬라이드당 최대 3회 시도, 탈락 사유를 반영해 프롬프트를 고쳐 쓴 뒤 재생성
- 프론트엔드에서 QA 진행 상황이 **실시간으로** 보임 (슬라이드별 상태 배지 + 시도 기록 로그)
- 실사용 검증 완료: 실제 공개 인사규정 PDF(K-water, 15p)로 5장 생성 성공

### 미구현 (범위 안이지만 아직 안 함)
- **결과물 다운로드** — MVP 범위에 넣어뒀지만 코드에 없음
- **DB 저장** — job 기록이 메모리에만 있어서 서버 재시작하면 이전 결과가 전부 사라짐 (SQLite로 붙일 예정)
- **README / GitHub 정리** — 저장소 연결은 했으나 README 없음

### 알려진 한계 (알면서 범위 밖으로 둔 것)
- **스캔본 PDF**(이미지만 있고 텍스트 레이어 없는 PDF)는 파싱 불가 — OCR 미도입
- **PDF 외 파일**(HWP, PPT, DOCX)을 올렸을 때의 처리 없음. 특히 정부 배포 문서가 HWP가 많음
- 파싱/생성 실패 시 사용자에게 보여줄 **에러 안내 UI**가 없음 (서버는 실패 상태를 반환하나 화면 처리 미흡)
- 이미지 안 텍스트의 **자모 단위 정확성은 AI가 스스로 검증 못 함** → 아래 "설계 원칙 2"로 우회함

### 측정 안 된 것 (다음에 채울 숫자)
- **1회 실행 비용** — 미측정. 재시도 루프가 곧 비용 배수라 `MAX_RETRIES=2`의 근거를 대려면 필요
- **QA 1회차 통과율 / 최종 탈락률 / 평균 시도 횟수** — 개별 실행 기록은 있으나 집계 안 함.
  "재시도 루프가 품질을 높인다"는 이 프로젝트의 핵심 주장을 증명할 유일한 수단
- **실행 시간**: 4장 기준 2분 24초 1건만 기록됨 (2026-08-17)

### 다음 할 일 (2026-08-20 재정렬)

**[1단계] 마무리 — 목표: "방어 가능한 완성품" 만들기 (예상 2~3일)**
1. **사람 승인 단계 추가** ⚠️최우선 — 기획안(`plan.json`)을 사람이 확인·수정한 뒤 이미지 생성으로 넘어가게. `run_pipeline`의 `on_plan_ready` 호출 지점이 그대로 이음매가 됨(`pipeline.py:82-86`). 이게 붙어야 "데모"에서 "제품 후보"가 됨
2. **README 작성** — GitHub에 올려둔 상태라 없으면 아무도 이해 못 함
3. **비용 1회 측정 + QA 통과율 집계** — 숫자 없이는 "QA 루프가 효과 있다"는 핵심 주장이 증명 안 됨. `MAX_RETRIES=2`의 근거도 여기서 나옴

**[2단계] 확장 — 둘 중 선택 (B 권장)**
- **(A) QA를 계산 가능한 검증으로 강화** → 아래 `확장 백로그 A` 참고. 기술 깊이 방향
- **(B) 영상 모드** (TTS + ffmpeg 슬라이드쇼 합성) → 원래 목표였고 미경험 기술. 포트폴리오 임팩트 큼

**[상시] 프로젝트 이해도 끌어올리기** ⚠️2026-08-20 추가 — 목표 80~90%
지인이 프로젝트를 물었을 때 제대로 답하지 못한 일이 계기. 원인 진단: 내용을 몰라서가 아니라 **설명을 직접 만들어본 적이 없어서**(인식과 인출은 다른 능력). 해결책은 설명을 더 듣는 게 아니라 **직접 말해보고 교정받는 것**. 세부 방법은 아래 `작업 방식` 섹션 참고.

**[나중]** 다운로드 기능 or 범위 제외 결정 / SQLite 도입 / 이력서용 카피 정리

---

## 작업 방식 (2026-08-20 변경)

**변경 이유**: 지금까지 코드를 대부분 AI가 작성하고 사용자는 확인만 하는 방식이었음. 결과물은 나왔지만 사용자가 로직을 스스로 설명하지 못하는 상태가 됨. 포트폴리오 프로젝트에서 이건 치명적 — 면접에서 설명 못 하면 만든 의미가 없음.

**새 방식 — 새 기능은 사용자가 직접 구현**
```
AI:    뭘 왜 바꿔야 하는지 설명 + 어디를 건드릴지 안내
사용자: 직접 작성
AI:    리뷰 + 틀린 부분 교정
```
첫 적용 대상: **사람 승인 단계** (파일 3개를 건드리는 적당한 크기, 기존 구조를 이해해야만 손댈 수 있음)

**설명 연습**: AI가 구현 의도를 질문하고("`on_attempt` 콜백이 왜 필요했나?") 사용자가 답하면 AI가 채점·보완. 막히는 지점이 곧 이해 안 된 지점이므로 진단 겸용.

### 대상별 설명 (준비해둘 것)

| 대상 | 길이 | 핵심 | 상태 |
|---|---|---|---|
| 지인(비개발자) | 30초 | 아래 스크립트 | ✅ 준비됨 |
| 면접 초반 | 3분 | 파이프라인 흐름 + QA 루프가 왜 특별한가 | 재료는 있음, 표현 미정리 |
| 면접 심화 | 10분+ | 실패 사례 3가지와 거기서 내린 판단 | 재료는 있음, 표현 미정리 |

**지인용 30초 스크립트**
> "회사에 있는 딱딱한 문서 있잖아, 취업규칙 같은 거. 그런 PDF를 넣으면 AI가 읽고 카드뉴스처럼 그림 슬라이드로 바꿔주는 걸 만들고 있어. 근데 그냥 만들기만 하는 게 아니라 — AI가 **자기가 그린 그림을 다시 보고** '이거 내용이랑 안 맞네' 하고 판단해서 **스스로 다시 그리게** 했어. 그게 핵심이야."

---

## 실행 방법

**백엔드 (FastAPI)** — 프로젝트 루트에서

```powershell
# PowerShell
.\venv\Scripts\Activate.ps1
uvicorn server:app --reload --port 8000
```

```cmd
:: cmd (명령 프롬프트)
venv\Scripts\activate.bat
uvicorn server:app --reload --port 8000
```

> ⚠️ 셸에 따라 활성화 스크립트가 다릅니다. cmd에서 `.ps1`을 실행하면 Windows가 이를
> "실행"이 아니라 "파일 열기"로 처리해서 **메모장이 뜹니다** (버그 아님).

**프론트엔드 (React + Vite)** — 새 터미널에서

```
cd frontend
npm run dev
```

접속: `http://localhost:5173` (백엔드는 `:8000`에 떠 있어야 함 — CORS가 5173만 허용)

**CLI로 파이프라인만 단독 실행**
```
python agents/pipeline.py sample_docs/sample_policy.pdf
```

**필요한 환경변수**: `.env` 파일에 `OPENAI_API_KEY=...` (`.env.example` 참고)

---

## 프로젝트 컨셉
**사내 문서 → 이해하기 쉬운 콘텐츠 자동 변환 에이전트** (B2B)

- 기업의 PDF/정책 문서/매뉴얼을 업로드하면 에이전트가 파싱 → 기획 → 이미지 생성까지 자동으로 수행
- 타겟: 중소~중견기업 HR/교육팀, SaaS 온보딩 담당자
- 페인포인트: 텍스트 자료는 이미 있는데 영상/이미지로 바꿀 인력·예산이 없음

**핵심 한 문장 (2026-08-16, 이력서/소개용으로 재사용할 것)**
> SinglePg는 텍스트를 이미지로 바꿔만 주는 도구가 아닙니다. 디자인 규격과 내용 정확도를 스스로 검수하고 교정하여, 사람이 바로 쓸 수 있는 완성도의 비주얼 콘텐츠를 완성해 내는 AI 에이전트입니다.

---

## 활용 시나리오 — 회사는 언제 텍스트를 시각화하는가 (2026-08-20 정리)

**근본 동인**: 문서가 없어서가 아니라 **있는데 안 읽혀서**. 회사에는 이미 취업규칙·매뉴얼·공지가 넘치지만 아무도 안 읽고, 그래서 "공지했는데 왜 몰랐냐 / 그런 거 있었냐"가 반복됨. 시각화는 문서를 만드는 일이 아니라 **읽히게 만드는 일**.

### 내부 커뮤니케이션 (직원 대상)

| 상황 | 원본 문서 | 반복성 | 이 제품 적합도 |
|---|---|---|---|
| **신입/경력 온보딩** | 취업규칙, 복무규정, 사내 시스템 안내 | 채용할 때마다 | ★★★ **최적** |
| **법정 의무교육** (성희롱예방·개인정보보호·산업안전·장애인인식개선·퇴직연금) | 교육 자료 PDF | **매년 전 직원 필수** (법적 의무) | ★★★ 반복성 최고, 단 정확성 리스크도 최고 |
| **제도 변경 공지** (인사제도 개편, 복지 변경, 근무제 전환) | 공지 문서 | 연 수회 | ★★☆ "공지 올렸는데 아무도 안 읽음" 페인 확실 |
| **보안/윤리 컴플라이언스** | 정보보안 수칙, 윤리강령 | 연 1~2회 | ★★☆ |
| **현장 안전수칙** (제조·건설) | 작업 안전 매뉴얼 | 상시 게시 | ★★★ 시각화 가치 최고 (외국인 노동자 등 텍스트 접근성 낮은 대상 포함) |

### 외부 커뮤니케이션 (고객·시장 대상)

| 상황 | 적합도 |
|---|---|
| SaaS 고객 온보딩 가이드 / FAQ 시각화 | ★★★ 문서 존재 + 반복 + 정확성 리스크 낮음 |
| 제품 매뉴얼 → 시각 가이드 | ★★☆ |
| 채용 브랜딩 (회사·복지 소개를 SNS 카드뉴스로) | ★★☆ |
| 영업 자료 요약 | ★☆☆ 고객별 커스터마이징 요구 큼 |
| **IR / 주주 커뮤니케이션** | ✗ **부적합** — 숫자가 콘텐츠의 핵심인데, 설계 원칙 2("숫자는 이미지에 안 넣는다")와 정면 충돌 |

### 진입 지점(beachhead) 판단

**1순위: 신입 온보딩.** 이유 — ① 원본 문서가 100% 이미 존재 ② 채용마다 반복 발생 ③ "신입이 취업규칙 안 읽는다"는 페인이 보편적 ④ 정확성 리스크가 법정교육보다 낮아 현재 완성도로도 시도 가능.

**2순위: 법정 의무교육.** 시장성은 가장 크지만(매년·전직원·법적 강제) **정확성 리스크가 커서 [실무 도입까지의 갭] (a) 사람 승인 단계가 반드시 선행**되어야 함.

**적합 조건 일반화**: ⓐ 원본 텍스트가 이미 있고 ⓑ 안 읽히는 문제가 실재하고 ⓒ 반복 발생하며 ⓓ 숫자가 콘텐츠의 주인공이 아닌 경우. 일회성 마케팅물은 디자이너를 쓰는 편이 낫고, 숫자 중심 자료는 이 제품 구조와 안 맞음.

---

## 시스템 구조

```
PDF 업로드
   │
   ▼
[1] 파싱      parser.py       PDF → 텍스트 (pypdf, 순수 기계적 추출)
   │
   ▼
[2] 기획      planner.py      텍스트 → 슬라이드 3~5장 구성안
   │                          (제목 / 나레이션 / 이미지 프롬프트 / key_text)
   ▼
[3] 이미지    visualizer.py   프롬프트 → PNG (gpt-image-1)
   │                          STYLE_GUIDE를 매 생성마다 강제로 덧붙임
   ▼
[4] QA        qa.py           이미지 검수 → 통과/탈락 판정
   │                          ① 홀리스틱 판정 (개념·장면 적합성)
   │                          ② blind 텍스트 판독 (맥락 없이 픽셀만 보고 옮겨 적기)
   │
   ├─ 통과 → 완료
   └─ 탈락 → visualizer.refine_image_prompt()로 프롬프트 수정 → [3]으로 (최대 3회)

        ↑ 이 루프 전체를 pipeline.py가 조립. 이게 이 프로젝트의 핵심 차별점.
```

**파일 역할**

| 파일 | 역할 |
|---|---|
| `agents/parser.py` | PDF → 텍스트 추출 |
| `agents/planner.py` | 텍스트 → 슬라이드 구성안 (`Slide`, `SlidePlan`) |
| `agents/visualizer.py` | 이미지 생성 + QA 탈락 시 프롬프트 재작성 |
| `agents/qa.py` | 이미지 검수 판정 (`evaluate_slide`) |
| `agents/pipeline.py` | 위 전체를 재시도 루프로 조립 (`MAX_RETRIES=2` → 총 3회 시도) |
| `server.py` | FastAPI. 업로드 → 백그라운드 실행 → 진행상태 조회 API |
| `frontend/src/App.jsx` | 3초 간격 폴링으로 진행상황 갱신 |
| `frontend/src/components/UploadForm.jsx` | 파일 업로드 폼 |
| `frontend/src/components/ProgressView.jsx` | 전체 진행률 표시 |
| `frontend/src/components/SlideTimeline.jsx` | 슬라이드별 상태 카드 (진행 중 → 완료 시 갤러리로 자연 전환) |
| `frontend/src/components/AgentLog.jsx` | 모든 시도 기록을 시간순 로그로 나열 |

**API**

| 엔드포인트 | 역할 |
|---|---|
| `POST /jobs` | PDF 업로드 → `job_id` 즉시 반환, 실제 처리는 백그라운드 |
| `GET /jobs/{id}` | 상태 + 슬라이드별 진행상황 조회 (프론트가 폴링) |
| `/files/{job_id}/...` | 생성된 이미지 정적 서빙 |

**결과물 저장 위치**: `outputs/{job_id}/slide_01.png ...` + `plan.json` (기획안 원본)

---

## MVP 범위 (v1)

**포함**
- 입력: PDF 문서 1개 업로드
- 파이프라인: 파싱 → 기획 → 이미지 프롬프트 생성 → 이미지 생성 → QA 재시도 루프
- 출력: **이미지 + 나레이션 캡션 세트** 3~5장, 웹 갤러리로 확인
- 최소 웹 UI (업로드 → 실시간 진행상황 → 결과 확인)

**제외 (v2 로드맵)**
- 영상 모드, PPT/HWP 파싱, 사용자 계정/결제, 브랜드 스타일 커스터마이징, 다국어

**"출력물"의 정의 (2026-08-15 명확화)**
최종 출력물은 **"이미지 단독"이 아니라 "이미지 + 나레이션 캡션 세트"**입니다. 카드뉴스가 원래 이미지+캡션이 한 세트인 형식이기 때문입니다.
- 정확한 수치·조건은 **캡션 텍스트가 책임짐** (코드가 렌더링하므로 100% 정확)
- 이미지는 전반적인 개념/분위기 전달 담당 (+ 아래 설계 원칙 2의 범위 내에서 짧은 제목 텍스트 통합)
- QA도 "이미지 단독"이 아니라 "이미지+나레이션 세트" 기준으로 판정함

**"캡션"이 정확히 무엇인가 (2026-08-20 명확화)**
캡션 = **이미지 아래에 HTML 텍스트로 렌더링되는 나레이션 문장**. 구현 위치는 `frontend/src/components/SlideTimeline.jsx`의 `<p>{slide.narration}</p>` 한 줄.

이미지 안의 글자와 캡션은 **생성 방식이 근본적으로 다르며**, 이 차이가 설계 원칙 2의 근거입니다.

| | 이미지 안 글자 | 캡션 |
|---|---|---|
| 만드는 주체 | AI가 픽셀로 **그림** | 브라우저가 폰트로 **렌더링** |
| 정확성 | "주 2일" → "주 2임"이 될 수 있음 | **100% 정확** (글자 데이터 그대로) |
| 수정 비용 | 이미지 재생성 필요 | 텍스트만 교체 |

즉 **"틀릴 수 있는 통로"와 "절대 안 틀리는 통로"를 분리하고, 중요한 정보는 안 틀리는 쪽으로만 보낸다**는 것이 이 구조의 핵심입니다.

---

## 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어 | Python | AI 에이전트 관련 SDK/튜토리얼 대부분 Python 우선 지원 |
| 프론트엔드 | **React + Vite** | 2026-08-15에 Streamlit에서 변경. 이력서 포트폴리오로서 완성도를 위해 (Streamlit은 "데모" 느낌). 대가로 API 서버·비동기 작업 처리·상태관리 복잡도를 감수 |
| 백엔드 | **FastAPI + uvicorn** | 기존 Python 파이프라인을 그대로 감쌈. 파이프라인이 몇 분 걸려서 `BackgroundTasks`로 비동기 실행 + 폴링 구조 필요 |
| 텍스트 LLM | OpenAI `gpt-5.4-mini` | 기획(planner) / QA 판정 / 프롬프트 재작성에 모두 사용. 비용·성능 균형 |
| 이미지 생성 | OpenAI `gpt-image-1` | 텍스트+이미지를 한 SDK로 처리 가능해 익힐 범위가 줄어듦 |
| 오케스트레이션 | 프레임워크 없이 직접 코드 | LangChain 등은 내부 동작을 가려서 이해에 방해됨. 목표가 "이해"이므로 직접 함수로 구현 |
| PDF 파싱 | **pypdf** | 가볍고 자료 많음 |
| job 상태 저장 | **메모리(dict)** | 로컬 데모 목적이라 과설계 방지. 서버 재시작 시 유실 감수 (→ 추후 SQLite) |

**응답 형식 강제**: OpenAI Responses API의 Structured Output(`text_format`에 pydantic 모델 전달) 사용. 자유 텍스트 응답을 다시 파싱하는 불안정한 방식 대신, 처음부터 정해진 JSON 스키마로만 답하도록 모델에 강제.

---

## 설계 원칙

이 프로젝트에서 시행착오 끝에 확정된 규칙들. **다시 논의가 나오면 처음부터 재검토하지 말고 여기부터 볼 것.**

### 원칙 1 — 슬라이드 개수는 "분량"이 아니라 "핵심 주제 개수"로 정한다 (2026-08-13)

처음엔 원본 PDF 페이지 수에 비례해서 슬라이드 개수를 정하려 했음 (예: 1~3페이지→3장, 10페이지 이상→5장). 하지만 사내 PDF의 실제 분량 분포를 알 수 없어 임계값 근거가 없었고, 논의 끝에 이 접근 자체를 폐기함.

**결정**: 슬라이드 개수는 원본 문서 분량과 무관하게 항상 3~5장 범위로 고정하고, 그 범위 안에서 "문서에 진짜로 구분되는 핵심 주제가 몇 개인지"를 LLM이 판단해서 정하게 함.

**Why**:
1. 이 제품이 파는 가치는 "분량"이 아니라 "시간"("3분 안에 훑어보기"). 슬라이드 수가 분량에 비례하면 긴 문서일수록 이 약속이 깨짐.
2. 실제 유사 서비스(Piktochart, Venngage 등)도 원본 분량과 무관하게 결과물 포맷을 고정하는 패턴을 씀.
3. 근거 데이터(실제 사내 PDF 분량 분포) 없이 임계값을 정하면 나중에 재조정이 불가피함. 고정 범위는 그 판단 자체가 필요 없음.

**How to apply**: `agents/planner.py`의 `plan_slides(min_slides=3, max_slides=5)`에 반영됨.
**검증됨**: 2026-08-18 K-water 인사규정(15p, 기존 샘플보다 훨씬 김)으로 실행 → 5장 생성. 분량이 늘어도 상한을 넘지 않음을 실사용으로 확인.

### 원칙 2 — 이미지 안 텍스트는 "틀려도 되는 것"만 넣는다 (2026-08-17)

**결정**: `key_text`(이미지에 통합해 그릴 텍스트)에는 **분위기용 짧은 제목만** 넣는다. (예: "재택근무", "연차 휴가")
**숫자·정확한 조건은 절대 넣지 않는다.** (예: "주 2일", "15일", "1주일 전") 정확한 수치는 코드가 렌더링하는 캡션이 100% 전담한다.

**Why**: `gpt-image-1`이 **그럴듯하지만 틀린 한글**을 그리는 사례를 실제로 확인함 ("주 2일" → "주 2임"). 더 심각한 건 이걸 **AI가 스스로 잡아내지 못한다**는 점이었음 — QA 검증을 두 번 갈아엎었는데 둘 다 실패:
1. 홀리스틱 QA에 "텍스트 옮겨 적기"를 시킴 → 나레이션이 맥락으로 같이 주어지니 모델이 실제 픽셀 대신 "문맥상 이래야 할 것 같은" 답을 함
2. 맥락을 완전히 제거한 별도 호출(`_transcribe_blind()`)로 분리 → 개선은 됐지만 자모 단위 정확도는 여전히 100% 보장 못 함

이 프로젝트의 핵심 가치가 "내용 정확도를 스스로 검수·교정"인데, **검증 신뢰도가 확인 안 된 채로 정보성 텍스트를 이미지에 맡길 수 없음.** 미적 개선(이미지-텍스트 통합감)이 정확성보다 우선할 수 없다는 원칙.

**How to apply**: `agents/planner.py`의 `Slide.key_text` 필드 설명과 `PLANNER_INSTRUCTIONS`에 명시됨. `agents/visualizer.py`의 `TEXT_INTEGRATION_GUIDE`가 실제 통합 지시를 담당.

---

## 경쟁 현황 & 차별화 (2026-08-12 조사)

이미 이 영역(문서→교육영상/인포그래픽)에 경쟁 서비스가 다수 존재함:
- 문서→영상: Synthesia, HeyGen, Golpo AI, Leadde, Knowlify (대부분 아바타 나레이션 중심)
- 문서→인포그래픽: SlideAI, Jeda.ai, Piktochart, Venngage, Coursebox AI

**전략**: MVP는 차별화 없이 기본 기능으로 빠르게 완성 → 이후 확장 단계에서 아래 차별화 포인트를 골라 반영

**차별화 포인트**
1. **QA 자체검토 루프** — 대부분의 경쟁 도구는 "1회 생성 후 사람이 수동 편집"하는 단발성 생성기. 우리는 결과물을 자체 평가하고 실패한 슬라이드만 재생성하는 진짜 에이전트 루프를 가짐 (**가장 중요한 차별점, MVP에 이미 구현 완료**)
2. **아바타 없는 다이어그램/일러스트 중심 시각화** — 경쟁 도구 다수가 "말하는 아바타" 영상 위주. 더 가볍고 저비용인 시각 설명 자료에 집중 *(v2 이후)*
3. **프라이버시/온프레미스 포지셔닝** — 민감한 사내 정책 문서를 외부 SaaS에 올리기 꺼리는 기업 대상 *(v2 이후)*

### "그냥 ChatGPT에 PDF 넣는 거랑 뭐가 달라?" (2026-08-20 정리)

**가장 자주 받게 될 질문이고, 정직하게 답하는 게 핵심.** 면접에서도 거의 확실히 나옴.

**먼저 인정할 것: 문서 1개 기준으로는 ChatGPT가 낫습니다.** 사람이 대화하며 "이건 더 밝게", "3번만 다시" 하고 즉석 조율하는 게 더 유연하고 결과도 좋을 가능성이 높음. 이걸 인정하는 데서 출발해야 진짜 차이가 보임.

**진짜 차이 — "대화"가 아니라 "시스템"**

| | ChatGPT | SinglePg |
|---|---|---|
| 검수하는 주체 | **사람** (내가 보고 "다시") | **코드** (자동 판정 → 자동 재생성) |
| 스타일 일관성 | 대화 흐름 따라 표류 | 매 호출마다 `STYLE_GUIDE` 강제 주입 |
| 제약 준수 | 매번 내가 기억해서 말해야 함 | 코드에 박혀 있어 **위반이 구조적으로 불가능** |
| 결과물 | 대화 기록 | `plan.json` + 시도 이력 = **데이터** |
| 문서 100개 | 세션 100번 × 사람 100번 | 업로드 100번, 사람 0번 |

**핵심 문장**:
> ChatGPT는 사람이 루프 **안**에 있고(human-in-the-loop), 이 프로젝트는 사람을 루프 **밖**으로 빼냈습니다(human-out-of-the-loop).

ChatGPT로 작업할 때 "이 그림 캡션이랑 안 맞네, 다시" 하고 판단하는 건 **사람**임. 이 프로젝트가 한 일은 **그 판단 자체를 코드로 옮긴 것** — `qa.py`가 사람의 눈 역할, `refine_image_prompt`가 "이래서 안 되니 이렇게 고쳐서" 역할.

의미가 있는 이유: **사람이 루프 안에 있으면 처리량이 사람 수에 묶임.** 문서 100개면 사람 시간 100번 필요. 루프 밖으로 빼면 그 제약이 사라짐. 이게 "AI를 쓰는 것"과 "AI 시스템을 만드는 것"의 차이.

**덤 — 제약을 코드에 박은 것의 힘**: 설계 원칙 2(숫자는 이미지에 안 넣기)를 ChatGPT에서 지키려면 매번 사람이 기억해서 말해야 하고, 한 번 까먹으면 잘못된 숫자가 박힘. 여기선 `planner.py` 스키마 설명에 박혀 있어 **까먹는 게 불가능**. 프롬프트 엔지니어링(사람이 매번 잘 쓰길 기대)과 시스템 설계(사람이 실수해도 규칙이 지켜짐)의 차이.

**면접용 답변 스크립트**:
> "문서 한 개만 놓고 보면 ChatGPT가 더 낫습니다. 사람이 대화로 조율하는 게 더 유연하니까요. 제가 만든 건 그 판단 과정을 코드로 옮긴 것이고, 그래서 사람 없이 반복 가능해집니다. 문서 100개를 처리해야 할 때 차이가 드러나는 구조예요. 다만 지금은 사람 승인 단계가 없어서 정확성이 중요한 HR 문서에 그대로 쓰기엔 위험하고, 그게 다음 과제라고 봅니다."

---

## 실무 도입까지의 갭 (2026-08-20 정리)

**결론: 지금 이대로는 실무 투입 불가. 포트폴리오로는 충분.**
아래 항목을 **본인 입으로 말할 수 있는 것 자체가 평가 포인트**임 — "실무에 쓸 수 있나요?"에 "네!"라고 답하는 것보다 한계를 정확히 짚는 쪽이 신뢰를 얻음.

| # | 갭 | 왜 치명적인가 |
|---|---|---|
| a | **사람 승인 단계 없음** ⚠️최우선 | HR 정책 문서는 틀리면 법적 문제가 되는 영역. planner가 "연차 15일"을 "5일"로 잘못 요약하면 그대로 사내 배포됨. 지금 파이프라인엔 사람이 검토·수정할 지점이 **한 군데도 없음**. 최소한 `plan.json`을 사람이 확인·수정한 뒤 이미지 생성으로 넘어가는 중간 관문이 필요 |
| b | **결과 수정 불가** | 기업 사용자는 반드시 결과를 고치고 싶어 함("3번 문구만", "이 그림만 다시"). 지금은 전체 재실행뿐 |
| c | **데이터 프라이버시** | 민감한 사내 인사 문서를 OpenAI 서버로 전송. 이것만으로 도입 불가인 기업 많음. (차별화 백로그 3번이 정확히 이 문제인데 아직 백로그 상태) |
| d | **HWP 미지원** | 한국 사내/정부 문서 상당수가 HWP. 실제로 고용노동부 표준 취업규칙을 HWP라서 못 읽고 K-water PDF로 대체한 사례 있음 (2026-08-18) |
| e | **출력 형식이 PNG뿐** | 실무자는 PPT/PDF로 받아 자기 자료에 붙이고 싶어 함 |

**그럼에도 방향 자체는 유효한 이유**
- 문제가 실재함: "텍스트 자료는 있는데 시각화할 인력이 없다"는 진짜 페인포인트이고, Synthesia·Piktochart 등이 이 시장에서 실제로 매출을 냄
- QA 루프는 실무 표준 패턴: LLM-as-judge, evaluator-optimizer 루프는 현재 프로덕션 AI 시스템에서 실제로 쓰는 구조. 장난감 패턴이 아님

---

## 확장 백로그 A — QA를 "계산 가능한 검증"으로 강화 (2026-08-20 착안, 미착수)

**핵심 통찰**: 이 프로젝트의 QA가 흔들렸던 근본 원인은 코드가 아니라 **검증할 객관적 기준이 없었다는 것**. "이 이미지가 좋은가?"에는 정답이 없어서 LLM 판단에 맡길 수밖에 없었고, LLM이 틀리면 잡아낼 방법이 또 없었음 (→ 설계 원칙 2의 후퇴로 이어짐).

**일반 원칙**: **에이전트 루프는 채점 기준이 코드로 확인 가능한 항목에 걸어야 강력해진다.** 그래야 "그럴듯해 보일 때까지"가 아니라 "진짜 맞을 때까지" 돌게 됨.

생성 프로젝트 안에도 계산 가능한 검증 항목이 존재하므로, 지금 전부 LLM에 맡긴 판정을 다음처럼 쪼갤 수 있음:

| 검사 항목 | 현재 | 바꿀 방법 |
|---|---|---|
| 색상 팔레트 준수 | LLM이 눈대중 | **픽셀 히스토그램 계산** — 지정 팔레트 벗어나면 탈락 |
| 글자/콘텐츠 잘림 | LLM이 봐야 앎 | **가장자리 픽셀 분석** — 이미지 경계에 콘텐츠가 물리면 탈락 |
| 슬라이드 간 스타일 일관성 | **검사 안 함** | **임베딩 유사도** — 한 장만 튀면 그 장만 재생성 |
| 이미지 안 텍스트 정확성 | 비전 LLM (2회 실패) | **전용 OCR 엔진** — 비전 LLM과 달리 문맥 추측을 하지 않고 픽셀만 봄 |

**마지막 항목이 특히 중요**: 비전 LLM이 "주 2일"을 잘못 읽은 원인이 *문맥으로 추측해서*였음. OCR 엔진은 접근 방식 자체가 달라(추측 없음) 이 벽을 뚫을 여지가 있음. → 성공하면 **설계 원칙 2(숫자 금지)를 완화할 수 있음.** 지금의 "검증이 안 되니 포기"는 검증 수단이 생기면 재검토 대상.

---

## 참고 (초기 기획 자료)

**검토했던 다른 후보**
1. 이커머스 상세페이지/광고소재 자동 생성 에이전트
2. 세일즈/HR용 맞춤 아웃바운드 영상 에이전트
3. **사내 문서 → 교육 콘텐츠 자동 변환 에이전트** ← 선택

**영상 모드 메모 (v2 스트레치)**
- 사용자는 영상 제작 경험이 전혀 없음 → 처음엔 이미지 모드로 에이전트 오케스트레이션 기본기부터 확실히 다지기
- 영상 = 꼭 AI 영상 생성 모델일 필요 없음. 더 쉬운 방식: 이미지 여러 장 + TTS 음성을 ffmpeg 같은 도구로 조립하는 "슬라이드쇼 합성" 방식부터 시작 가능 (AI 영상 생성보다 훨씬 진입장벽 낮음)

**프로젝트 목표**
단순히 데모 완성이 아니라, 만드는 과정에서 사용하는 기술/용어를 제대로 이해하는 것이 궁극적 목표. 설계 단계부터 개념 설명을 곁들여서 진행.
사용자 배경: Python/JS 둘 다 만져봤지만 설명은 자신없는 수준(표면적 경험). OpenAI, Gemini API 키 보유.

---

# 진행 로그

> 아래는 **수정하지 않는 기록**입니다. 새 작업은 맨 아래에 덧붙이고, 결과로 바뀐 "결정"은 위쪽 섹션에 반영합니다.

- 2026-08-12: 컨셉 확정(문서→교육콘텐츠 에이전트), 경쟁사 조사 완료, MVP 범위 확정, 기술 스택 확정.
- 2026-08-12: 프로젝트 골격 구축 완료
  - 폴더 구조: `agents/`(에이전트 모듈), `sample_docs/`(테스트용 문서), `venv/`(가상환경, git 제외)
  - `requirements.txt`: streamlit, openai, pypdf, python-dotenv
  - `.env.example` 작성 (실제 `.env`는 사용자가 직접 생성, OpenAI 키 보유)
  - `.gitignore` 작성 (venv, .env, outputs 등 제외)
  - `agents/parser.py` 구현 — PDF → 페이지별 텍스트 추출 (순수 기계적 추출만 담당, 의미 구조화는 다음 단계인 planner.py 몫)
  - 샘플 정책 문서 PDF(`sample_docs/sample_policy.pdf`)로 파싱 테스트 완료, 정상 동작 확인
- 다음 단계: `agents/planner.py` — 추출된 텍스트를 OpenAI로 슬라이드 단위 기획(제목+핵심포인트+나레이션풍 문장)으로 구조화하는 에이전트 구현
- 2026-08-13: OpenAI 키 연결 확인 완료. `agents/planner.py` 구현 및 테스트 완료
  - OpenAI Responses API의 Structured Output(`text_format`에 pydantic 모델 전달) 사용 — 자유 텍스트 응답을 다시 파싱하는 불안정한 방식 대신, 처음부터 정해진 JSON 스키마(Slide: title/narration/image_prompt)로만 답하도록 모델에 강제
  - 사용 모델: `gpt-5.4-mini` (텍스트 기획용, 비용/성능 균형)
  - 샘플 정책 문서로 테스트 → 영어 원문을 한국어 나레이션 슬라이드 4장으로 잘 구조화함을 확인
- 2026-08-13: `agents/visualizer.py` 구현 및 전체 파이프라인(파싱→기획→이미지생성) 테스트 완료
  - 사용 모델: `gpt-image-1`, 사이즈 1024x1024, 응답은 항상 b64_json (url 아님)
  - 모든 슬라이드 프롬프트 뒤에 공통 `STYLE_GUIDE` 문구를 덧붙여 스타일 일관성 확보 — 심플 플랫 벡터, 톤 통일된 색상(블루/그레이/화이트), 이미지 내 텍스트 금지, "예쁨보다 이해"가 최우선이라는 제약을 매 생성마다 강제
  - 샘플 문서로 4장 생성 → 톤/그림체 통일, 텍스트 없이 아이콘 조합만으로 내용 전달 확인 (성공)
  - 출력 위치: `outputs/slide_01.png` ~ (git 제외 대상)
- 다음 단계: QA 자체검토 루프 (`agents/qa.py`) — 생성된 슬라이드가 원래 의도(핵심 주제, 이해도)에 맞는지 자체 평가하고, 부족한 슬라이드만 재생성. 이후 Streamlit UI(`app.py`)로 전체 파이프라인을 웹에서 실행/확인할 수 있게 묶기
- 2026-08-13: `agents/qa.py` 평가 함수(`evaluate_slide`) 구현 및 단독 테스트 완료
  - 방식: 이미지+원래 제목/나레이션을 멀티모달 LLM(`gpt-5.4-mini`)에 함께 보내 Structured Output(`QAResult: passed, reason`)으로 판정
  - 평가 기준 3가지: (1) 3초 안에 핵심이 전달되는가 (2) 하나의 명확한 주제만 담았는가 (3) 이미지 안에 글자/숫자가 없는가
  - 설계 이유: 재시도 루프와 얽히기 전에 판정 로직만 먼저 검증 — 판정이 부정확하면(너무 관대/너무 엄격) 재시도 루프를 만들어도 무의미하거나 무한 재생성으로 비용만 나갈 수 있음
  - 테스트: 기존 생성된 슬라이드 4장 재사용(이미지 재생성 없이 `plan.json`만 다시 뽑아 매칭, 비용 절감). 결과 — 3장 통과, 1장(재택근무 슬라이드) 실제로 탈락. 탈락 사유도 타당함(나레이션의 핵심 조건이 그림에 안 드러남, UI 텍스트처럼 보이는 요소). 판정 로직이 무분별하게 통과시키지 않는다는 걸 확인
  - `agents/visualizer.py`에 `plan.json` 저장 로직 추가 — 이미지와 슬라이드 원문을 나중에 다시 짝지을 수 있도록
- 다음 단계: 탈락 슬라이드만 골라 재생성하는 재시도 루프 (최대 재시도 횟수 제한 포함), 이후 파싱→기획→이미지→QA 전체를 하나로 묶는 파이프라인 함수, 그다음 Streamlit UI
- 2026-08-14: `agents/pipeline.py` 구현 — 파싱→기획→이미지생성→QA→(탈락 시)프롬프트 보정→재생성을 하나로 묶음. 프로젝트의 핵심 차별점("진짜 에이전트")이 여기서 완성됨
  - `agents/visualizer.py`에 `refine_image_prompt(slide, previous_prompt, qa_reason)` 추가 — QA 탈락 사유를 LLM에게 다시 주고 image_prompt를 고쳐 쓰게 함 (같은 프롬프트로 무작정 재시도하지 않음)
  - `build_slide()`: 슬라이드 1장당 최대 3회 시도(`MAX_RETRIES=2`), 매 시도마다 QA 판정 로그 기록
  - 샘플 문서로 전체 파이프라인 실행 결과: 슬라이드 1 (1회 통과), 슬라이드 4 (3회차에 통과), 슬라이드 2·3은 **3회 모두 탈락** (한도 초과로 종료) — 재시도 루프의 종료 조건(무한루프 방지)이 의도대로 작동함을 확인
  - **발견 1 — QA 기준의 내재적 모순**: 슬라이드 2("주 2일까지", "1주일 전 신청"), 슬라이드 3("15일, 최대 5일 이월") 둘 다 정확한 수치 규정이 핵심인 슬라이드인데, QA 기준 3("이미지 안에 글자/숫자 금지")과 기준 1("3초 안에 핵심 전달")이 서로 충돌함. 아이콘 개수로 수치를 암시하는 정도로는 통과 기준을 못 채움.
    - 원인 진단: QA가 "이미지 단독"으로 판단하게 설계했는데, 실제 완성 슬라이드는 항상 이미지+나레이션 캡션이 같이 보임. 정확한 수치는 나레이션 텍스트가 담당하면 되므로, QA 기준 자체가 실사용 맥락보다 과도하게 엄격했을 가능성이 큼. → 다음에 재검토 필요 (미해결)
  - **발견 2 — 재시도할수록 스타일이 STYLE_GUIDE에서 벗어남**: 슬라이드 2·3의 최종 이미지가 어둡고 뿌연 비네트 배경으로 나옴 (원래 목표는 밝은 플랫/블루·그레이·화이트 톤). STYLE_GUIDE 문구는 매 생성마다 코드에서 붙이는데도, `refine_image_prompt`가 "더 명확하게 강조해라" 식의 지시를 추가하면서 이미지 모델이 스타일보다 강조를 우선시한 것으로 추정. → 프롬프트 보정 시 STYLE_GUIDE를 "불변 제약"으로 명시적으로 재강조하는 수정 필요 (미해결)
- 2026-08-15: 위 두 미해결 이슈 모두 해결 및 재검증 완료
  - **이슈 1 해결**: `agents/qa.py`의 QA 기준 1을 "이미지 단독으로 정확한 정보 전달" → "이미지가 나레이션 캡션과 나란히 놓였을 때 전반적 개념/맥락을 뒷받침하는가"로 수정. 근거: 최종 결과물은 항상 이미지+캡션 세트(카드뉴스)로 노출되므로, 정확한 수치는 캡션 텍스트가 책임지면 됨 — 이미지 단독 평가가 애초에 실사용 맥락보다 과했음
  - **이슈 2 해결**: `agents/visualizer.py`의 `refine_image_prompt` 컨텍스트에 STYLE_GUIDE를 명시적으로 포함하고 "스타일(색감/조명/배경톤)은 절대 변경 금지, 장면 구성만 조정" 지시 추가. 근본 원인은 프롬프트를 재작성하는 LLM이 애초에 STYLE_GUIDE 존재 자체를 몰랐던 것 — 판단을 내리는 컴포넌트에 필요한 제약을 실제로 넘겨야 한다는 일반 원칙 적용
  - 재검증 결과: 샘플 문서 4장 모두 통과 (슬라이드 2는 3회차에 통과 — 이번엔 화면 속 실제 텍스트("HR PORTAL")가 찍힌 정당한 사유였고 기준 3이 정상 작동함을 재확인). 스타일 드리프트도 재발 안 함
- **명확화 (2026-08-15)**: "출력물"이 이미지 단독이 아니라 "이미지+나레이션 캡션 세트"라는 정의를 명시함 (MVP 범위 섹션에 기록). 나레이션 텍스트는 지금까지 QA 판정용 내부 자료로만 쓰였는데, 실제로는 최종 화면에 캡션으로 노출돼야 하는 필수 요소. 프론트엔드 구현 시 각 슬라이드를 "이미지 카드 + 캡션 텍스트"로 렌더링해야 함
- **스택 변경 결정 (2026-08-15)**: UI를 Streamlit 대신 **React + Vite(프론트) + FastAPI(백엔드, 기존 Python 파이프라인 감쌈)**로 변경. 이유: 이력서 포트폴리오로서 완성도(Streamlit은 "데모" 느낌)를 위해. 트레이드오프: API 서버, 비동기 작업 처리(파이프라인이 몇 분 걸림 → 백그라운드 작업+진행상태 polling 구조 필요), 프론트엔드 상태관리 등 복잡도 증가 감수. 추후 필요시 Next.js로 전환 가능(마이그레이션 비용 낮음, 앱 규모가 작음)
- 다음 단계: FastAPI 백엔드로 파이프라인 감싸기(업로드→백그라운드 실행→진행상태/결과 조회 API) → React+Vite 프론트엔드(업로드 폼, 진행률, 이미지+캡션 갤러리)
- 2026-08-15: `server.py`(FastAPI 백엔드) 구현 및 실제 업로드→완료까지 end-to-end 테스트 완료
  - `requirements.txt`에서 streamlit 제거, fastapi/uvicorn/python-multipart 추가
  - `agents/pipeline.py::run_pipeline`에 `on_plan_ready`, `on_slide_done` 콜백 파라미터 추가 — 파이프라인 코드 자체는 그대로 두고, 각 단계가 끝날 때마다 외부(서버)에 알림만 주는 구조. 서버가 이 콜백으로 진행 상황을 기록해서 프론트가 폴링할 수 있게 함
  - API: `POST /jobs` (PDF 업로드 → job_id 즉시 반환, 실제 처리는 FastAPI `BackgroundTasks`로 백그라운드 실행) / `GET /jobs/{id}` (상태·슬라이드별 진행상황 조회) / `/files/{job_id}/...` (생성된 이미지를 정적 파일로 서빙)
  - job 상태는 별도 DB 없이 메모리(dict)에 저장 — 로컬 데모 목적이라 서버 재시작 시 기록 유실 감수, 과설계 방지
  - CORS는 로컬 Vite 개발 서버(`localhost:5173`) 오리진만 허용하도록 설정
  - curl로 실제 샘플 PDF 업로드 → 상태 pending→running→done 전이, 슬라이드 4장 순차 반영, 이미지 정적 서빙(`/files/{job_id}/slide_01.png`, 200 image/png) 전부 확인
- 다음 단계: React + Vite 프론트엔드 — 업로드 폼, 진행률 표시(폴링), 슬라이드를 "이미지 카드 + 나레이션 캡션" 세트로 보여주는 갤러리
- 2026-08-15: `frontend/` (React + Vite) 구현 완료
  - `src/api.js`: 백엔드 호출 함수(createJob, getJob, fileUrl)
  - `src/components/UploadForm.jsx`, `ProgressView.jsx`, `SlideGallery.jsx`(이미지+나레이션 캡션 카드로 렌더링)
  - `App.jsx`: job_id 발급 후 3초 간격으로 `GET /jobs/{id}` 폴링, 완료되면 갤러리 표시
  - 기본 Vite 템플릿 잔재(카운터 버튼, 로고 등) 전부 제거하고 프로젝트 전용 UI로 교체
- 2026-08-15: Playwright로 브라우저 기반 end-to-end 테스트 수행 (프로젝트에 별도 run 스킬이 없어서 즉석에서 chromium 드라이빙 스크립트 작성)
  - 업로드 폼 렌더링, 파일 선택, 변환 시작 클릭, 진행률 표시, 갤러리 렌더링까지 실제 브라우저로 확인. 콘솔 에러 없음
  - 처음엔 스크린샷에 이미지가 빈 화면으로 나와서 원인 조사 → 실제로는 이미지 정상 로드(200, complete:true, naturalWidth:1024)였고 스크린샷을 너무 일찍 찍은 테스트 스크립트 타이밍 문제였음(실제 버그 아님)
  - 재검증 중 **OpenAI 크레딧 소진**(429 insufficient_quota)으로 슬라이드 4 생성 중 파이프라인 실패 발생 — 오늘 반복 테스트로 크레딧을 다 쓴 것. 다만 이 실패가 오히려 서버의 에러 처리 경로(job.status="failed" + error 메시지 전달)가 정상 작동함을 확인시켜줌
  - **막힌 지점**: OpenAI 계정 크레딧 충전 필요 (https://platform.openai.com/settings/organization/billing) — 사용자 액션 필요, 충전 후 전체 파이프라인 1회 재검증하면 프론트엔드까지 마무리됨
- 2026-08-16: OpenAI 크레딧 충전 확인됨 (API 정상 호출 재확인)
- 2026-08-16: **QA 루프를 프론트엔드에 실시간으로 노출하는 기능 추가** (Gemini로 만들어본 UI 컨셉 목업에서 착안 — "PDFToon" 목업의 실시간 QA 타임라인 아이디어가 우리 프로젝트 핵심 차별점과 정확히 맞아떨어져서 채택)
  - `agents/pipeline.py::build_slide`에 `on_attempt(index, attempt, result, is_last_attempt)` 콜백 추가 — 슬라이드 하나가 다 끝난 뒤가 아니라, 시도 하나가 끝날 때마다(재시도 도중에도) 알림
  - `server.py`: `JobState.slides`를 기획 완료 직후 전체(3~5장) "생성 중" 상태로 미리 채워두고, 시도마다 상태(`generating`/`passed`/`failed_final`)와 QA 사유 히스토리(`attempts: [{attempt, passed, reason}]`)를 갱신. 이미지 URL에 `?v={attempt}` 붙여서 재생성 시 브라우저가 캐시된 이전 이미지를 계속 보여주지 않게 함
  - 프론트: `SlideGallery.jsx`를 `SlideTimeline.jsx`로 교체 — 진행 중/완료 상태를 하나의 컴포넌트로 통일해서, 처리 중엔 실시간 타임라인으로 보이다가 완료되면 자연스럽게 최종 갤러리가 됨. 카드에 상태 배지(생성중/통과/검수기준미달)와 탈락 시 "QA 탈락: {사유} → 재생성 중..." 텍스트 표시
  - Playwright로 재검증: 슬라이드 상태가 실시간으로 생성중→통과 전환되는 것 확인(중간 스냅샷으로 캡처), 콘솔 에러 없음, 4장 모두 최종 통과. **크레딧 충전 후 최종 파이프라인 재검증도 이 테스트로 겸함 — 완료**
- 2026-08-16: **프론트엔드 비주얼 디자인 1차 적용** (Gemini로 만든 "SinglePg" 컨셉 목업을 레퍼런스로 참고, 색/구조만 채택하고 대시보드형 구조는 MVP 범위 밖이라 제외)
  - 남색(`--navy`) 마스트헤드 바 추가 — 로고 마크 + "SinglePg" 워드마크 + 태그라인
  - 업로드/결과 영역을 흰 카드 패널로 분리하고, 상단에 옅은 블루 톤(`--band-bg`) 밴드 라벨("1. 문서 업로드", "2. 에이전트 작업 현황"/"결과 갤러리")을 붙여 단계 구분을 명확히 함
  - 상태 배지 색을 시맨틱하게 정리: 생성중=블루, 통과=초록, 최종탈락=빨강(기존 amber에서 변경)
  - 신규 `AgentLog.jsx` — 모든 슬라이드의 시도 기록(통과/탈락+사유)을 슬라이드 번호순·시도순으로 나열하는 로그 패널, 통과/탈락에 따라 색 구분. 백엔드가 이미 갖고 있던 `attempts` 데이터를 재사용만 하면 돼서 구현 비용 낮았음
  - Playwright로 idle 화면 + 실제 파이프라인 실행 화면 둘 다 재검증. 콘솔 에러 없음. 이번 실행에서는 슬라이드 1이 3회 재시도 후에도 최종 탈락(이미지에 읽을 수 있는 글자 흔적 남음)했는데, 이 케이스에서 빨간 배지+탈락사유 박스가 의도대로 렌더링되는 것까지 확인 — 실패 케이스 스타일링도 검증됨
- 2026-08-17: **"이미지+텍스트가 따로 노는 느낌" 문제 논의 시작** — outputs 폴더를 파일로 직접 보면 어떤 슬라이드인지 설명이 없다는 지적에서 출발
  - 검토한 방법들: (A) 별도 요약 문서, (B) 다운로드용 HTML 미니페이지, (C) SVG(이미지+실제 텍스트 결합), (D) Pillow로 캡션을 이미지에 합성. 사용자가 전 프로젝트(광고 이미지 생성)에서 D 방식(safe zone + 글자수 제한 + PIL 합성)을 써봤는데 "이미지와 텍스트가 따로 노는 느낌"이 강했다고 함 — 그래서 포스트프로세싱 합성 계열(C, D, F) 전체를 배제하고 다른 방향을 찾기로 함
  - 대안으로 나온 방향: (1) 이미지 생성 AI에게 짧은 제목까지 장면의 디자인 요소로 통합 생성하게 하기, (2) 이미지 먼저 생성 후 `images.edit`(inpainting)으로 텍스트를 자연스럽게 얹기 — 단, 이것도 같은 모델을 쓰므로 글자 깨짐 리스크는 여전히 있고 QA+재시도로 관리해야 함, (3) 숫자/핵심어를 텍스트가 아니라 조형물(입체 오브젝트)로 그리기 — "글자 금지" 규칙과 충돌 안 하면서 정보 전달력 강화
  - **실험 결과 (매우 긍정적)**: `gpt-image-1`로 테스트 이미지 3장 생성 — ① 조형물 스타일 숫자 "2" 성공(왜곡 없이 깔끔), ② 두 자리 숫자 "15" 성공(1, 5 모두 정확히 읽힘), ③ 한글 단어 "재택근무"를 포스터 헤드라인처럼 장면에 통합 생성 — **완전히 안 깨지고 정확하게 렌더링됨** (다만 상단 여백 부족으로 글자 일부가 캔버스 밖으로 잘림 — 프롬프트에 여백 지시 추가하면 해결 가능한 사소한 구도 문제)
  - 결과 파일: `outputs/text-test/num_2.png`, `num_15.png`, `korean_word.png`
  - **이건 처음에 QA 규칙("이미지 안에 글자/숫자 절대 금지")을 정할 때 전제했던 "AI는 한글 텍스트를 못 그린다"는 가정 자체를 재검토할 근거가 됨**
  - **다음에 이어서 할 일 (미결정 상태로 중단)**:
    1. 표본이 3장뿐이라 우연일 수 있음 — 같은 프롬프트로 2~3번씩 더 생성해서 일관성 확인 필요
    2. 일관성 확인되면 실제 파이프라인(`planner.py`의 image_prompt 생성 방식, `qa.py`의 "글자 금지" 규칙)에 반영할지 결정
    3. 반영하기로 하면: planner가 나레이션에서 핵심 숫자/짧은 제목을 뽑아 image_prompt에 "장면에 통합해서 그려라" 지시를 추가하는 방향 + QA 기준에 "통합된 텍스트가 정확히 읽히는가"를 추가하는 작업 필요
- 2026-08-17: **이미지 텍스트 통합 기능 구현 완료 — 단, 안전 범위로 제한**
  - `planner.py`: `Slide`에 `key_text: str | None` 필드 추가. 나레이션에서 분위기용 짧은 제목(2~4단어)이 있으면 채우고, image_prompt에는 그 텍스트를 언급하지 않게 함(통합 지시는 코드가 별도로 붙임)
  - `visualizer.py`: `TEXT_INTEGRATION_GUIDE` 추가 — key_text가 있으면 "포스터 헤드라인처럼 통합해서, 잘리지 않게 여백을 두고" 그리라는 지시를 프롬프트에 자동으로 덧붙임. `refine_image_prompt`에도 key_text 컨텍스트 전달(재시도 시에도 텍스트 유지)
  - `qa.py`: 텍스트 정확성 검증 로직을 두 번 갈아엎음
    - 1차 시도: 홀리스틱 QA 응답에 `detected_text` 필드를 추가해 "옮겨 적어라" → 실패. 나레이션이 같이 주어지니 모델이 "문맥상 이래야 할 것 같다"고 실제 픽셀 대신 그럴듯한 추측을 함 (실제로 "주 2일"을 "주 2임"으로 잘못 그렸는데도 통과 처리된 사례 발견)
    - 2차 시도: `_transcribe_blind()`를 완전히 별도 API 호출로 분리, 나레이션/기대 텍스트 등 아무 맥락 없이 이미지만 보고 텍스트를 옮겨 적게 함. 이것도 여전히 완벽하지 않음(사용자 육안 확인으로 "주2일"이라 읽어냈지만 실제로는 "주2임"으로 보임 — blind 방식도 자모 단위 정확도까지는 100% 보장 못 함)
  - **최종 결정 (안전 범위로 축소)**: 두 번의 검증 강화 시도에도 AI가 스스로 자모 단위 정확성을 100% 신뢰성 있게 검증하지 못한다는 게 확인됨 → **숫자/정확한 조건은 절대 key_text에 넣지 않기로 함** (예: "주 2일", "15일" 금지). key_text는 "틀려도 치명적이지 않은 분위기용 제목"에만 한정(예: "재택근무", "연차 휴가"). 정확한 수치는 지금처럼 100% 코드가 렌더링하는 캡션 텍스트가 전담
    - 이유: 이 프로젝트의 핵심 가치("내용 정확도를 스스로 검수·교정")를 지키려면, 검증 신뢰도가 확인 안 된 채로 정보성 텍스트를 이미지에 맡길 수 없음. 미적 개선(이미지+텍스트 통합감)이 정확성보다 우선할 수 없다는 원칙 적용
  - planner.py 재검증: 숫자 없는 분위기용 제목만 key_text에 채워짐 확인 ("유연 근무", "재택근무", "연차 휴가", 없으면 null)
  - 전체 파이프라인 최종 재검증: 4장 모두 1회차에 통과, 총 소요시간 2분 24초. 제목 텍스트("원격근무", "연차 휴가")가 포스터 헤드라인처럼 자연스럽게 통합되어 렌더링됨, 오류 없음
- 2026-08-18: **실제 공개 PDF로 첫 실사용 테스트** — 한국수자원공사(K-water) 공개 인사규정 PDF(약 15페이지, 조항 구조)를 웹 UI로 업로드해 실행
  - 고용노동부 "표준 취업규칙"도 검토했으나 HWP 형식으로만 배포돼(`pypdf`로 못 읽음) 제외, 대신 PDF로 바로 공개된 K-water 문서를 사용
  - 결과: 5장 생성됨(기존 샘플 정책 문서는 4장이었음) — 슬라이드 개수가 문서 분량이 아니라 LLM이 판단한 핵심 주제 개수로 정해진다는 설계 원칙이 분량이 훨씬 긴 실제 문서에서도 그대로 작동함을 처음으로 실사용 데이터로 확인
  - 슬라이드 2가 1회차 QA 탈락 후 정상적으로 2회차 재시도로 넘어가는 것 확인 (`server.py`의 `on_attempt`: 탈락+마지막 시도 아님 → 상태 `generating` 유지하고 재생성, 마지막 시도까지 탈락해야 `failed_final`로 확정) — 재시도 루프가 실사용 흐름에서도 의도대로 동작함
  - 로컬 개발 환경 이슈: VSCode 터미널이 cmd(명령 프롬프트)일 때 `venv\Scripts\Activate.ps1`(PowerShell 전용)을 실행하면 cmd가 `.ps1`을 몰라서 파일 연결 프로그램(기본값 메모장)으로 열어버려 notepad가 뜨는 현상 발견 — cmd에서는 `venv\Scripts\activate.bat`을 써야 함 (실제 버그 아니고 셸 종류에 따라 실행 명령이 다름)
  - **다음에 필요할 것으로 논의됨 (아직 미착수)**: 지금은 job 상태를 메모리(dict)에만 저장해서 서버 재시작하면 이전 결과 기록이 사라짐 — "예전 결과 다시 보기" 같은 기능이 필요해지면 DB 필요. 무거운 DB는 과설계이므로 **SQLite**(파일 하나로 끝나는 경량 DB) 정도로 job 메타데이터만 저장하는 방향으로 합의. 이미지 파일 자체는 지금처럼 `outputs/` 폴더에 그대로 둠. 시점은 MVP 마무리(README/GitHub 올리기) 단계쯤으로 잠정 — 아직 실제 착수는 안 함
- 2026-08-18: git 저장소 초기화 + GitHub 원격 연결(`kartia02/pdf_to_img`). `.gitignore`에 `uploads/`, `*.log`, `frontend/node_modules/`, `frontend/dist/` 추가 (기존 `venv/`, `.env`, `__pycache__/`, `outputs/`에 더해서)
- 2026-08-18: **문서 구조 전면 정리** — 기획서로서 신뢰할 수 있게 재구성
  - 문제 진단: 진행 로그가 시간순으로 덧붙기만 해서 뒤 내용이 앞 내용을 조용히 무효화하고 있었음. 실제로 기술 스택 표는 아직 "UI: Streamlit"이라고 적혀 있었고(08-15에 React로 바뀜), MVP 범위는 "이미지 안 텍스트 금지"였으며(08-17에 부분 허용으로 바뀜), "다운로드"는 범위에 있으나 코드에 없었고, 완료된 "다음 단계:" 항목 5개가 그대로 남아 있었음
  - 해결: 문서를 **결정 층**(현재 사실만, 바뀌면 덮어씀)과 **경위 층**(진행 로그, 절대 수정 안 함)으로 명시적으로 분리. 진행 로그는 한 줄도 수정하지 않고 그대로 보존
  - 신설: `현재 상태`(동작하는 것 / 미구현 / 알려진 한계 / 측정 안 된 것 / 다음 할 일), `실행 방법`, `시스템 구조`(파이프라인 흐름도 + 파일 역할표 + API 표)
  - 통합: 흩어져 있던 설계 결정을 `설계 원칙` 섹션 아래 2개로 모음 (원칙 1: 슬라이드 개수 기준, 원칙 2: 이미지 안 텍스트 안전 범위)
- 2026-08-20: **프로젝트의 정체성·시장성을 정면으로 재검토** (코드 변경 없음, 전부 문서 작업)
  - 계기: 사용자가 "막상 만드니 그냥 ChatGPT에 PDF 넣고 이미지 생성해달라는 거랑 똑같은 것 같은데 무슨 차이지?"라고 질문. 프로젝트의 존재 이유를 묻는 가장 중요한 질문이라 정면으로 답하고 문서화함
  - **결론(정직한 버전)**: 문서 1개 기준으로는 ChatGPT가 더 낫다는 걸 먼저 인정. 진짜 차이는 결과물이 아니라 **결과물이 만들어지는 방식** — ChatGPT는 사람이 루프 안에서 판단하고(human-in-the-loop), 이 프로젝트는 그 판단을 코드로 옮겨 사람을 루프 밖으로 뺐음(human-out-of-the-loop). 처리량이 사람 수에 묶이지 않게 되는 지점에서 차이가 발생. → `경쟁 현황 & 차별화` 섹션에 표·핵심문장·면접용 스크립트까지 정리
  - **실무 도입까지의 갭 5가지를 명시적으로 문서화**: (a) 사람 승인 단계 없음 ⚠️최우선 (b) 결과 수정 불가 (c) 데이터 프라이버시 (d) HWP 미지원 (e) 출력 PNG뿐. "지금 이대로는 실무 투입 불가, 포트폴리오로는 충분"이라는 판단을 문서에 박아둠 — 한계를 정확히 짚는 것 자체가 평가 포인트라는 판단
  - **활용 시나리오 섹션 신설**: "회사는 언제 텍스트를 시각화하는가"를 내부/외부 커뮤니케이션으로 나눠 정리하고 각각 적합도 평가. 진입 지점은 **1순위 신입 온보딩**(문서 존재+반복+정확성 리스크 상대적으로 낮음), **2순위 법정 의무교육**(시장성 최대지만 갭 (a) 선행 필요). IR/주주 자료는 숫자가 콘텐츠의 주인공이라 설계 원칙 2와 충돌하므로 **부적합**으로 명시
  - `캡션`의 정의를 MVP 범위 섹션에 명확화 — 이미지 안 글자(AI가 픽셀로 그림, 틀릴 수 있음)와 캡션(브라우저가 폰트로 렌더링, 100% 정확)의 차이가 설계 원칙 2의 근거라는 점을 표로 정리
- 2026-08-20: **프로젝트를 갈아엎을지 진지하게 고민 → 계속 진행하기로 결정** (코드 변경 없음)
  - 계기: 사용자가 "처음엔 주제가 재밌어 보여서 가벼운 마음으로 시작했는데, **실제로 사용한다는 전제로 만들려니 시야가 달라진다**"며 주제를 새로 정할지 고민한다고 밝힘
  - **진단 — 이 감각은 주제 탓이 아니라 단계 탓**: 8일 전엔 안 보이던 결함(승인 단계 없음, HWP 미지원, 수정 불가)이 지금 보이는 건 프로젝트가 나빠진 게 아니라 보는 눈이 자란 것. 중요한 건 **주제를 바꿔도 8일 뒤 똑같은 벽을 만난다**는 점 — 모든 프로젝트는 현실에 대보면 장난감처럼 보이고, 실제 제품은 그 벽을 하나씩 메꿔서 되는 것이지 벽 없는 주제를 잘 고른 게 아님. 갈아타면 시계만 리셋되고 "벽을 넘어본 경험"이 계속 안 쌓임
  - 또한 **포트폴리오는 "팔리는 제품"으로 평가되지 않음** — 면접관이 보는 건 판단 능력이고, "실무에 쓸 수 있나요?"에 갭 5가지를 정확히 짚어 답하는 것 자체가 이미 그 증거임
  - **결정적 입력**: 사용자가 전 프로젝트(광고 이미지 생성)부터 **생성(이미지/영상) 분야 자체를 좋아해서** 이 주제를 골랐다고 밝힘 → 혼자 하는 포트폴리오에서 동기는 핵심 조건이므로, 앞서 대안으로 제시했던 "비정형 문서 → 구조화 데이터 추출"(견적서 숫자 추출 등)은 **철회**. 기술적으로는 좋은 선택이지만 흥미와 안 맞아 마무리가 안 될 위험이 큼
  - **결론: SinglePg 계속 진행.** 단 "계속할까 새로 할까"가 아니라 **"마무리하고 다음으로"**의 순서 문제로 재정의 — 지금 갈아타면 90%짜리 미완성 하나가 남지만, 마무리하면 완성품 하나 + 다음 프로젝트가 되고, "이걸 만들며 이런 한계를 발견해 다음엔 이렇게 했다"는 **성장 서사**가 생김
  - 로드맵 확정: **[1단계] 마무리**(사람 승인 단계 → README → 비용·통과율 측정, 2~3일) → **[2단계]** (A) 계산 가능한 검증으로 QA 강화 / **(B) 영상 모드 ← 권장**
  - (B)를 권하는 이유: 노트 초기에 "영상은 이미지 모드로 에이전트 오케스트레이션 기본기를 다진 뒤"라고 적어뒀는데 **그 기본기가 지금 완성됨**. 원래 계획한 타이밍이 도래했고, 이미 슬라이드 이미지+나레이션 텍스트가 있어 TTS+ffmpeg 합성으로 자연스럽게 확장 가능(AI 영상 생성 모델이 아니라 슬라이드쇼 합성이라 진입장벽 낮음). 승인 단계가 영상에서 **더** 중요해지는 것도 1단계를 먼저 하는 근거 — 영상은 재생성 비용이 커서 사람이 기획안을 먼저 확인해야 함
  - 부산물: "검증에 정답이 필요하다"는 이번 프로젝트 최대 교훈을 생성 분야 안에서 적용하는 방법을 정리 → `확장 백로그 A` 신설
